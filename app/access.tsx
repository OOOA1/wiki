"use client";
import {useState} from 'react';
import {BookOpen,KeyRound,ArrowRight,Lock,RefreshCw} from 'lucide-react';
import {api} from '@/lib/client';
export default function Access({signInHref,name='',error}:{signInHref?:string;name?:string;error?:string}){const [displayName,setName]=useState(name);const [token,setToken]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);return <main className="access-page"><div className="access-box"><div className="brand"><span className="brand-icon"><BookOpen/></span>Кафедра</div><h1>{error?'Вики временно недоступна':signInHref?'Знания, которые нас объединяют':'Добро пожаловать в команду'}</h1><p>{error|| (signInHref?'Войдите в свой аккаунт, чтобы открыть общую вики программистов и преподавателей.':'Введите токен, который вам передал администратор. Приглашение будет привязано к вашему аккаунту.')}</p>{error?<button className="primary-button" onClick={()=>location.reload()}><RefreshCw size={16}/>Попробовать ещё раз</button>:signInHref?<a href={signInHref} target="_top" className="primary-button">Войти через ChatGPT<ArrowRight size={17}/></a>:<form className="form-stack" onSubmit={async e=>{e.preventDefault();setBusy(true);setMessage('');try{await api({action:'accept_invitation',name:displayName,token:token.trim()});location.reload()}catch(e){setMessage((e as Error).message)}finally{setBusy(false)}}}><label><span className="field-label">Ваше имя</span><input className="field-input" value={displayName} onChange={e=>setName(e.target.value)} required maxLength={70} autoComplete="name"/></label><label><span className="field-label">Токен приглашения</span><input className="field-input" value={token} onChange={e=>setToken(e.target.value)} placeholder="kfd_…" required autoComplete="off" spellCheck={false}/></label>{message&&<div role="alert" className="error-banner">{message}</div>}<button className="primary-button" disabled={busy}><KeyRound size={16}/>{busy?'Проверяем токен…':'Присоединиться к вики'}</button></form>}<p className="access-footnote"><Lock size={12} style={{display:'inline',marginRight:5}}/>Статьи и файлы доступны только участникам команды.</p></div></main>}
